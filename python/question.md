1. Dictionary aggregation  
   Given a list of dictionaries with title and amount, combine amounts by title (like your example).

2. Word frequency counter  
   Write a function that takes a string and returns the count of each word. Example: "hello world hello" → {"hello": 2, "world": 1}.

Remove duplicates from list  
Given a list with repeated elements, return a new list with unique elements while preserving order.

Find second largest number  
Given a list of integers, find the second largest without using sorted() directly.

Group anagrams  
Given a list of words, group them into sets of anagrams. Example: ["eat","tea","tan","ate","nat","bat"].

JSON flattening  
Write a function to flatten a nested dictionary into a single level with keys joined by dots. Example:

python
{"a": {"b": 1, "c": 2}} → {"a.b": 1, "a.c": 2}
File line count  
Write a script to count the number of lines in a text file without using len(open().readlines()).

FizzBuzz variation  
Print numbers from 1–50, but replace multiples of 3 with "Fizz", multiples of 5 with "Buzz", and multiples of both with "FizzBuzz".
